"use client";

import ProfileSettings from "@/components/profile-settings";

export default function ProfileSettingsClient() {
  // Add router and handlers as needed for your ProfileSettings component
  return <ProfileSettings user={{}} onBack={() => {}} onSave={async () => {}} onSignOut={() => {}} />;
}
